Make sure you have python 3.6+ installed
Then install anaconda for python3 to provide for a safe sandbox for the script to run, although it can also be done without anaconda
Then simply run 

pip install -r requirements.txt

from this folder

List of dependencies

firebase
firebase_admin
python-firebase
getpass
python_jwt
gcloud
sseclient
